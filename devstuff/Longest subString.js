// Function that returns longest substring from initial string without repeated symbols

const stringToBeChecked = 'Some string, that should be checked, must be written first. There could be any stuff like blablablabla, \n aijfiaejfjeoo and everything you might dream about!';
const newString = '';
// console.log(stringToBeChecked);
const checkForSubStrings = (someStr) => {
    let a = b;
    return;
}